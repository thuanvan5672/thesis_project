# Thesis Project - API Neo4j + MongoDB

Flask API kết nối:
- MongoDB Atlas (dữ liệu trái cây / sản phẩm)
- Neo4j Aura (graph trái cây, xuất xứ, công dụng, ...)

## Chạy local

```bash
python -m venv .venv
.\.venv\Scripts\activate  # Windows
pip install -r requirements.txt
python app.py
